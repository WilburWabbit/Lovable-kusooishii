# Kuso Oishii — Build Chat Transcript (Part 2)

**Range**: Messages 197–256
**Theme**: QBO VAT/TaxCode integration, admin table infrastructure, role-based admin tab, customer sync planning, sales sync rate-limiting, Doc Number column.

---

## Message 197 — User — 2026-03-10 08:13

Lovable plan: Approved.

Plan to audit monetary values and VAT linkage across QBO sync functions. Adds `tax_total`, `global_tax_calculation` to `inbound_receipt`; `qbo_tax_code_ref` to `inbound_receipt_line`; `global_tax_calculation` to `sales_order`; `qbo_tax_code_ref`, `vat_rate_id` (FK → vat_rate) to `sales_order_line`. Updates `qbo-sync-purchases` and `qbo-sync-sales` to capture `GlobalTaxCalculation`, `TxnTaxDetail.TotalTax`, line-level `TaxCodeRef`. For sales, computes `merchandise_subtotal` based on TaxInclusive vs TaxExcluded.

## Message 198 — Assistant — 2026-03-10 08:16

Implemented QBO tax data integration:
- Migration adding `tax_total`, `global_tax_calculation`, `qbo_tax_code_ref`, `vat_rate_id` columns.
- `qbo-sync-purchases`: captures `GlobalTaxCalculation`, `TotalTax`, line-level `TaxCodeRef`.
- `qbo-sync-sales`: handles TaxInclusive vs TaxExcluded; resolves `vat_rate_id` from `TaxRateRef`; same logic for `RefundReceipt`.
- Registered `qbo-sync-sales` in `supabase/config.toml` with `verify_jwt = false`.

```typescript
if (globalTaxCalc === "TaxInclusive") {
  merchandiseSubtotal = totalAmount - taxTotal;
  grossTotal = totalAmount;
} else {
  merchandiseSubtotal = totalAmount;
  grossTotal = totalAmount + taxTotal;
}
```

## Message 199 — User — 2026-03-10 08:17

Show the tax_total and global_tax_calculation columns on the Intake page receipts table so admins can see the tax treatment at a glance.

## Message 200 — Assistant — 2026-03-10 08:17

Added Tax and Tax Treatment columns to the Intake receipts table with badge formatting for Inclusive/Excluded.

## Message 201 — User — 2026-03-10 08:26

Update the intake, inventory, and orders UI to all show the vat rate %, VAT amount per line, unit prices net VAT, and totals vat inclusive. No need to include a tax treatment column as then as the tax is always displayed the same way regardless of the backend data.

## Message 202 — Assistant — 2026-03-10 08:27

Proposed plan: Orders page gets VAT% / VAT / Line Total inc VAT columns; Intake page apportions `tax_total` pro-rata across stock lines; Inventory clarifies "Landed (net)". Notes that intake lines have no `vat_rate_id` FK — only `qbo_tax_code_ref`.

## Message 203 — User — 2026-03-10 08:29

Lovable plan: Declined.

## Message 204 — Assistant — 2026-03-10 08:30

Asked clarifying questions: simpler scope, different display, or different data approach for Intake.

## Message 205 — User — 2026-03-10 08:33

In QBO there is a difference between TaxCode and TaxRate. We've created a table for tax rate but I don't understand the relationship QBO uses between the two. Can you investigate and tell me how we can track both so we can always calculate tax at stock unit level up?

## Message 206 — Assistant — 2026-03-10 08:34

Investigated QBO tax model. Two-tier system: TaxCode is a grouping per line, containing `SalesTaxRateList` and `PurchaseTaxRateList` referencing TaxRate entities. Proposed `tax_code` table with FKs to `vat_rate` for sales and purchase rates. Stock unit → receipt/order line → `tax_code_id` → `vat_rate.rate_percent`.

```sql
CREATE TABLE tax_code (
    id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    qbo_tax_code_id text UNIQUE NOT NULL,
    name text NOT NULL,
    active boolean DEFAULT TRUE,
    sales_tax_rate_id uuid REFERENCES vat_rate(id),
    purchase_tax_rate_id uuid REFERENCES vat_rate(id),
    synced_at timestamptz DEFAULT now()
);
```

## Message 207 — User — 2026-03-10 08:36

Lovable plan: Approved. (TaxCode plan above)

## Messages 208–216 — TaxCode implementation

Created `tax_code` table; added `tax_code_id` FK to `inbound_receipt_line` and `sales_order_line`; built `qbo-sync-tax-codes` edge function; integrated TaxCode resolution into purchase/sales syncs; added Tax Codes tab to VAT Rates settings page with sortable table.

## Message 217 — User — 2026-03-10 10:27

Lovable plan: Approved. Sortable, Filterable, Configurable Admin Tables with Persistence.

Plan: shared `useTablePreferences` hook (localStorage-keyed), `ColumnSelector` popover, `SortableTableHead` wrapper, shared `sortRows` utility. Retrofit Inventory, Orders, Intake, VAT Rates, Tax Codes, Users pages.

## Message 218 — Assistant — 2026-03-10 10:33

Built shared infrastructure (`useTablePreferences`, `SortableTableHead`, `ColumnSelector`, `table-utils.ts`) and retrofitted all 5 admin pages. Each uses unique `tableId` like `admin-inventory` for localStorage persistence.

## Message 219 — User — 2026-03-10 11:30

Tied to user role, for Admins and Staff only, display another option at the end "Admin" that will take the user to /admin. The button must not display at all for members.

## Message 220 — Assistant — 2026-03-10 11:32

Updated `useAuth` to expose `roles` array and `isStaffOrAdmin`. Conditionally rendered Admin tab on AccountPage using Shield icon, navigating to `/admin`.

## Message 221 — User — 2026-03-10 11:35

How can we get all of the inventory updated with the correct VAT code and rates to calculate them per stock item?

## Message 222 — Assistant — 2026-03-10 11:36

Investigation: 673 stock units missing `inbound_receipt_line_id`; 468 receipt lines missing `tax_code_id`. Raw payloads contain `TaxCodeRef`. Proposed backfill in `qbo-sync-purchases`: parse `raw_payload`, resolve TaxCodeRef → tax_code.id, link stock_unit → receipt_line by mpn + condition_grade.

## Message 223 — User — 2026-03-10 11:36

Lovable plan: Approved.

## Message 224 — Assistant — 2026-03-10 11:38

Implemented `backfillProcessedReceipt` in `qbo-sync-purchases`. Added `inbound_receipt_line_id` to new stock_unit inserts. Toast in `QboSettingsPanel` shows `backfilled_tax_codes` and `backfilled_stock_links` counts.

## Message 225 — User — 2026-03-10 11:41

Let's build the QBO customer sync. QBO is the master for basic customer records. Orders, regardless of channel, need to be attributed to a QBO customer record and data that can be stored in QBO should be stored there. Anything else needs to be stored in the app.

## Message 226 — Assistant — 2026-03-10 11:42

Asked clarifying questions: which QBO customer fields are master? Backfill existing orders? Create admin Customers page?

## Message 227 — User — 2026-03-10 11:43

QBO master fields: All of the above. Backfill orders: Yes. Admin UI: Yes, full page.

## Message 228 — Assistant — 2026-03-10 11:43

Proposed plan:
- New `customer` table: `qbo_customer_id`, `display_name`, `email`, `phone`, `mobile`, billing address fields.
- Added `customer_id` FK to `sales_order`.
- New `qbo-sync-customers` edge function.
- Backfill linkage during sync via `CustomerRef.value`.
- New `/admin/customers` page using shared table infrastructure.
- Sync Customers button in `QboSettingsPanel`.

## Messages 229–236 — Customer sync implementation

Created `customer` table + RLS; built `qbo-sync-customers` edge function pulling up to 1000 customers with full address mapping; updated `qbo-sync-sales` to resolve `CustomerRef.value` → `customer.id`; backfill query for existing orders; created `/admin/customers` page with sortable/filterable table; added Sync Customers button to QboSettingsPanel; updated OrdersPage to display linked customer names.

## Message 237 — User — 2026-03-10 12:43

Lovable plan: Approved. Fix QBO Sales Sync: Rate Limiting.

Plan: reduce item fetch concurrency (BATCH_SIZE 5→2, delay 250ms→500ms), add retry-with-backoff on 429, throttle VAT backfill (500ms between receipts), don't cache 429 failures, add 20s time budget for backfill.

## Message 238 — Assistant — 2026-03-10 12:44

Implemented all rate-limiting fixes in `qbo-sync-sales`:
- `fetchQboItem` retries once after 2s on 429, doesn't cache 429 failures.
- Item pre-fetch BATCH_SIZE=2, 500ms delay.
- Backfill loop throttles 500ms between receipts, breaks on 429, has 20s time budget.

```typescript
if (res.status === 429) {
  if (attempt === 0) {
    await new Promise(r => setTimeout(r, 2000));
    continue;
  }
  return null; // Don't cache 429 failures
}
```

## Message 239 — User — 2026-03-10 12:51

VAT is showing on orders when they are expanded, but the table line item is not showing the correct figures. Note, the date of order is also wrong for all records.

## Message 240 — Assistant — 2026-03-10 12:52

Identified two issues: (1) table rows use `sales_order.tax_total` which was stored as 0 for TaxInclusive transactions; expanded view correctly resolves via `tax_code → sales_tax_rate`. (2) `sales_order` has no `txn_date` — only `created_at` (sync time). Proposed: compute Net/VAT/Total from line data in frontend; add `txn_date` column with regex backfill from notes.

## Message 241 — User — 2026-03-10 12:53

Lovable plan: Approved.

## Message 242 — Assistant — 2026-03-10 12:54

Implemented:
- Added `txn_date` column to `sales_order`; backfilled via `regexp_match(notes, 'on (\d{4}-\d{2}-\d{2})')`.
- `qbo-sync-sales` now stores `TxnDate` for sales and refund receipts.
- `admin-data` includes `txn_date` in list-orders.
- `OrdersPage` computes Net/VAT/Total from line items; displays `txn_date` formatted as `dd MMM yyyy`.

```typescript
const orderNetFromLines = (o: OrderRow) =>
  o.sales_order_line.reduce((s, l) => s + l.line_total, 0);
const orderVatFromLines = (o: OrderRow) =>
  o.sales_order_line.reduce((s, l) => s + (lineVatAmount(l) ?? 0), 0);
```

## Message 243 — User — 2026-03-10 12:59

Some orders are not showing any VAT details, even though they are present in QBO. Add the QBO Doc Number to the UI as Sales Receipt and use that as the primary column instead of the apps internal Order #.

## Message 244 — Assistant — 2026-03-10 13:00

Proposed plan: add `doc_number` column to `sales_order`; backfill via `regexp_match(notes, '#(.+?) on ')`; store `receipt.DocNumber` during sync; increase `BACKFILL_TIME_BUDGET_MS` from 20s to 45s; replace primary column with "Sales Receipt".

## Message 245 — User — 2026-03-10 13:01

Lovable plan: Approved.

## Message 246 — Assistant — 2026-03-10 13:02

Implemented:
- Migration: added `doc_number` column; backfilled from notes regex.
- `qbo-sync-sales`: stores `DocNumber`; backfill budget extended to 45s.
- `admin-data`: includes `doc_number` in list-orders select.
- `OrdersPage`: replaced "Order #" column with "Sales Receipt" mapped to `doc_number`; updated sort and search filters.

---

(Messages 247–256 truncated in chunked fetch — see PART_03 for continuation.)
