## Problem\n\nThe Specifications tab is correctly rendering every active row in `canonical_attribute`, but the seeded registry has wiring that doesn't match the real `product` schema. As a result:\n\n- **EAN** is configured but its provider chain works — it just looks blank for any product without one (only 7 of 390 have it). Confirmed working in principle.\n- **Weight** has two duplicate canonical attributes (`weightG` + `weight_grams`); one reads `product.weight_g` (correct), the other reads `product.weight_grams` (column doesn't exist).\n- **Length / Width / Height** registry entries point at `length_mm` / `width_mm` / `height_mm` but the real product columns are `length_cm` / `width_cm` / `height_cm`. Result: always \